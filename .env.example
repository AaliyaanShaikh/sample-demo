# Optional: when the app is hosted under a subpath, set the public base (see FrameScroll URLs).
# NEXT_PUBLIC_BASE_PATH=

# Gemini API key for the on-site chat assistant — https://aistudio.google.com/apikey
# GEMINI_API_KEY=
